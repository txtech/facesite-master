<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nabobsite.modules.nabob.cms.sys.dao.SequenceCodeDao">
	
	<!-- 查询数据
	<select id="findList" resultType="SequenceCode">
		SELECT ${sqlMap.column.toSql()}
		FROM ${sqlMap.table.toSql()}
		<where>
			${sqlMap.where.toSql()}
		</where>
		ORDER BY ${sqlMap.order.toSql()}
	</select> -->

	<insert id="addSequence">
		insert into t1_sequence_code ( name ) values (  0 )
	</insert>

	<select id="getNextSequence" resultType="java.lang.Long">
		select IFNULL(max(id),0) from t1_sequence_code
	</select>
</mapper>