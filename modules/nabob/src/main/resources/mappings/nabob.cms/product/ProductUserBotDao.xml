<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nabobsite.modules.nabob.cms.product.dao.ProductUserBotDao">
	
	<!-- 查询数据
	<select id="findList" resultType="ProductUserBot">
		SELECT ${sqlMap.column.toSql()}
		FROM ${sqlMap.table.toSql()}
		<where>
			${sqlMap.where.toSql()}
		</where>
		ORDER BY ${sqlMap.order.toSql()}
	</select> -->

	<update id="updateProductUserBotJob" parameterType="com.nabobsite.modules.nabob.cms.product.entity.ProductUserBot">
		update
			t1_product_user_bot
		<set>
			yesterday_team_income_money = today_team_income,
			yesterday_income_money = today_income_money,
			today_team_income = 0,
			today_income_money = 0,
			today_orders = 0
		</set>
	</update>
</mapper>