<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nabobsite.modules.nabob.cms.product.dao.ProductUserBotAistartDao">
	
	<!-- 查询数据
	<select id="findList" resultType="ProductUserBotAistart">
		SELECT ${sqlMap.column.toSql()}
		FROM ${sqlMap.table.toSql()}
		<where>
			${sqlMap.where.toSql()}
		</where>
		ORDER BY ${sqlMap.order.toSql()}
	</select> -->

	<select id="getProductUserBotAistartList" parameterType="com.nabobsite.modules.nabob.cms.product.entity.ProductUserBotAistart">
		SELECT
			${sqlMap.column.toSql()}
		FROM
			${sqlMap.table.toSql()}
		where
			daily_num &lt;= 10 and ai_status !=3  AND now() BETWEEN start_date AND end_date
	</select>
</mapper>